{
  "name": "overwatch-cod",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OVERWATCH — a browser FPS engineered to stand next to a modern Call of Duty. Three.js r180, WebGL2, zero art assets; every texture, mesh and sound generated procedurally at load time.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "capture": "node tools/capture.mjs",
    "shotset": "node tools/shotset.mjs",
    "baseline": "node tools/shotset.mjs --baseline",
    "imagediff": "node tools/imagediff.mjs",
    "gate": "npm run baseline && npm run shotset && npm run imagediff"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
